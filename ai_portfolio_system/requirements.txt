# Minimal runtime dependencies (runs offline with Mock LLM).
# Uses SQLite + numpy as the default vector store and scikit-learn TF-IDF fallback for embeddings.

pydantic>=2.0
pyyaml>=6.0
python-dotenv>=1.0
numpy>=1.24.0
scikit-learn>=1.3.0

# TUI (Codex-style terminal popup)
textual>=0.79.0

# SQL Agent
sqlalchemy>=2.0.0

# Optional: Web search
# duckduckgo-search>=6.0

# Optional: production-grade vector DB + embeddings (heavy, requires torch).
# Install with: pip install -r requirements-full.txt
# chromadb>=0.5.0
# sentence-transformers>=3.0

# Optional: LLM providers.
# langchain-openai>=0.2.0
# langchain-community>=0.3.0
